import { join } from 'path';

export const COMPONENT_NAME = 'multi-scf';

export const VERSION_YAML_PATH = join(__dirname, '..', 'version.yml');

export const COMPONENT_CODE_PATH = join(__dirname, '..', 'build');
